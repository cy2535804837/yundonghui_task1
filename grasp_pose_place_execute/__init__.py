"""
Place a held object: move the arm that grasped it from home to a target
placement pose and release. Reuses the motion + gripper primitives from
grasp_pose_grasp_execute and reads the grasp handoff to pick the arm.
"""
